# 近期模式证券新鲜度过滤设计

## 目标

在“近期相似区间”模式中，排除最后交易日早于目标区间结束日前第 30 个交易日的证券，避免退市、长期停牌及遗留日线文件进入结果。

## 设计

以目标证券自身交易日序列为市场日历。目标结束位置向前移动 30 个索引得到最早允许日期；不足 31 根数据时使用目标证券首日。近期模式读取候选证券后、生成滑窗前检查其最后交易日，早于该日期则跳过。历史滑窗与同期联动保持不变。

将截止日期计算提取为纯函数并导出到测试 API。回归测试覆盖正常目标区间、目标历史不足 30 日，以及候选证券边界日期的接受与拒绝。
