{
  "name": "kline-similarity-tool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/algorithm.test.cjs tests/integration.test.cjs"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
